# Vinterum. Education assistant

AI mentor using agent orchestration and Retrieval-Augmented Generation (RAG)

Implemented functions:
 - Ingests Markdown (.md) course materials into a RAG knowledge base
 - Performs semantic search + answer generation grounded in retrieved context
 - Routes to a human tutor when the answer cannot be reliably verified

